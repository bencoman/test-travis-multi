language: c

osx_image: 
  - xcode7.3

dist:
  - trusty
  - precise

os:
  - windows
  - linux
  - osx 

env:
  - MATRIX=32x86/squeak.cog.spur
  - MATRIX=32x86/pharo.cog.spur
  - MATRIX=64x64/squeak.cog.spur
  - MATRIX=64x64/pharo.cog.spur

jobs:
  include:
    - stage: Cog builds
      env: FLAVOR=cog.spur
      name: "Cog"
    - stage: Stack builds
      env: FLAVOR=stack.spur
      name: "Stack"

script:
  - uname -a
  - env
  - find / -maxdepth 2 -print -name proc -prune || true
  # - find /proc -maxdepth 2

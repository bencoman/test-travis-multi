language: c

os:
  - windows
  - linux
  - osx

osx_image: xcode7.3

env:
  - DIALECT=squeak
  - DIALECT=pharo

matrix:
  - FLAVOR=cog.spur
  - FLAVOR=stack.spur

script:
  - uname -a
  - env
  - find / -maxdepth 2 -print -name proc -prune || true
  # - find /proc -maxdepth 2
